from setuptools import setup, find_packages
import sys, os

version = '0.1'

setup(name='salsa',
      version=version,
      packages=['Salsa'],
      description="",
      keywords='',
      author='',
      author_email='',
      license='',
      scripts=[
          'salsa/bin/salsa'
      ],
      install_requires=[],
      )
